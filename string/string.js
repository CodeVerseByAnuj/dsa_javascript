// string is immuitable: it means no change the value.
// lenth property:str.length
//slice:str.slice(1,4) it starts index start but last works 1 index below. ye -1 se bhi index leta h back side se.
  

let str = "anuj";
str[2]="r"
console.log(str.length);